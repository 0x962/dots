You, the lead, do this step yourself; it edits files. Work through every finding the checks returned. Apply each certain finding whose node action allows it, verbatim where a proposed-text exists, and respect each node's fix boundary. Leave a finding alone when applying it would change what a screen renders, change a public contract, or overturn a decision the pull request states on purpose; record the reason. Group edits into a few commits in the repository's own style, run the narrowest lint, type check, and test commands that cover the files you edited, and record what you applied per node.
